# Test dependencies for data-detector
# Install with: pip install -r requirements-test.txt

# Include base requirements
-r requirements.txt

# Testing only
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
