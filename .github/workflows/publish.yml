# DEPRECATED: This workflow is deprecated in favor of the unified release.yml workflow
# which handles TestPyPI -> PyPI publishing with validation.
# This workflow is kept for manual TestPyPI testing only.
name: Manual TestPyPI Publish (Deprecated)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual TestPyPI publish'
        required: true
        type: string

jobs:
  build-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Log manual publish reason
        run: |
          echo "⚠️  Manual TestPyPI publish triggered"
          echo "Reason: ${{ inputs.reason }}"

      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Clean old build artifacts
        run: |
          echo "🧹 Cleaning old build artifacts..."
          rm -rf dist build *.egg-info
          find . -type d -name "*.egg-info" -exec rm -rf {} +

      - name: Upgrade build toolchain
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade setuptools wheel build twine

      - name: Build fresh distributions
        run: python -m build

      - name: Dump METADATA and PKG-INFO
        run: |
          python - <<'PY'
          import glob, zipfile, tarfile
          for f in glob.glob("dist/*.whl"):
              print("=== WHEEL:", f)
              with zipfile.ZipFile(f) as z:
                  meta = [n for n in z.namelist() if n.endswith("METADATA")][0]
                  print(z.read(meta).decode())
          for f in glob.glob("dist/*.tar.gz"):
              print("=== SDIST:", f)
              with tarfile.open(f, "r:gz") as t:
                  pkgi = [m for m in t.getmembers() if m.name.endswith("PKG-INFO")][0]
                  print(t.extractfile(pkgi).read().decode())
          PY

      - name: Validate metadata
        run: twine check dist/*

      - name: Upload to TestPyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.TEST_PYPI_API_TOKEN }}
        run: twine upload --repository-url https://test.pypi.org/legacy/ dist/*
